# QEMU images are large binary blobs built locally from the QNX SDK; never commit.
# (The recommended workflow builds into ~/qnx8-qemu, outside the repo entirely.)
*.img
*.ifs
*.bin
*.vmdk
*.qcow2
output/
local/
